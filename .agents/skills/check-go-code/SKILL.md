---
name: check-go-code
description: Run Go tests, linters, and checks using Dagger scafolding
---

Use this skill when testing, validating, or checking Go code.

---

Use Dagger with `dagger check --silent --cloud` when all checks should be run.

* To run just tests: Use `dagger call test --silent --cloud`
